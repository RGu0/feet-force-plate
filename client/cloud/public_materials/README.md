# 受控公开材料

此目录保存项目需要长期保留、但不包含任何私密材料的公开密码学资料。

`license-public.key.base64` 是 RAY-321 历史公开集成投递中的 32 字节 Ed25519
License 验签公钥的标准 Base64 表示。其 SHA-256、编码和用途由同目录 JSON 元数据
固定；使用前必须解码为原始 32 字节并核对 SHA-256。

该密钥只用于验证 License，绝不能用作 Windows 云默认配置批准签名的验签信任根。
RAY-321 的批准验签公钥须由负责人另行提供并固定在受控客户端源码中。
